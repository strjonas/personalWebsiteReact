Goal:
folders with bookmarks and folders in them
-> folder in folder structure possible



backend: 

default folder = main;
every folder has one row with i = folders; // contains a text like this:
     folder1,folder2,... , it is used to show which folders are in this folder


sql row: 

id primary key, // for identifing the bookmarks
link text, // the link of the bookmarks
folder text, // folder of the row
i varchar(255), // is folder or bookmark


erst wird geguckt, welche folder sind im mainFolder, dafuer sucht man nach dem eintrag
mit i = folder and folder = main;
dann zeigt man alle folder als ein ein/ausfahr ding an
dann guckt man welche bookmarks im main folder sind und ziegt sie untereinander an
dann bekommt jeder folder seine bookmarks und folder, und zeigt sie an
das laeuft so ab:
folder wird geoeffnet -> query"folders and links where folder = {foldername}
-> elemente.map() 


frontend:

link component
folder component

